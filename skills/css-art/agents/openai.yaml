interface:
  display_name: "CSS Art"
  short_description: "Trace reference images into standalone pure HTML and CSS art."
  default_prompt: "Use $css-art to trace my reference image into a faithful standalone HTML/CSS illustration."
policy:
  allow_implicit_invocation: true
